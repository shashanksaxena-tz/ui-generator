# Magic UI Components

This directory contains components from https://magicui.design/

## Installation

Components are copy-pasted and adapted for our schema system. Magic UI is a copy-paste component library (similar to shadcn/ui), so we don't install a package but rather copy individual components.

Required dependencies (framer-motion) are already installed.
