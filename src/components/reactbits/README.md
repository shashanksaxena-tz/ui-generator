# React Bits Components

This directory contains components from https://reactbits.dev/

## Installation

Components are copy-pasted from the website. React Bits is a copy-paste component library, so we don't install a package but copy individual components.

Required dependencies (framer-motion) are already installed.
