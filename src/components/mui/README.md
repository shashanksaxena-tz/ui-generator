# Material UI Components

This directory contains selected components from Material UI.

We import these from @mui/material and wrap them for our schema system.

## Installation

Material UI is installed as a package dependency.

Required dependencies:
- @mui/material
- @mui/icons-material
- @mui/x-data-grid (for DataGrid component)
- @emotion/react
- @emotion/styled
