# Chakra UI Components

This directory contains selected components from Chakra UI.

We import these directly from @chakra-ui/react and wrap them for our schema system.

## Installation

Chakra UI is installed as a package dependency (@chakra-ui/react).

Required dependencies:
- @chakra-ui/react
- @emotion/react
- @emotion/styled
